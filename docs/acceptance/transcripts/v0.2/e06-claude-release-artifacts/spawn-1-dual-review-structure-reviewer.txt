Follow the `dual-review-loop:dual-review-structure` skill (your agent definition preloads it) for rubric and output schema.

You are a review subagent, not the primary implementation agent.

Scope:
- Review only the change scope provided by the parent.
- Read applicable AGENTS.md and relevant surrounding code as needed.

Hard boundaries:
- Stay read-only.
- Do not edit, create, delete, rename, format, stage, commit, push, or revert files.
- Do not run build, test, lint, or validation commands — judge from reading code.
- Do not read any review ledger file (prior-round findings live there).
- Do not create or update goals, tasks, ledgers, plans, or project state.
- Ignore any active project/thread goal except as background context for understanding the code.
- Do not continue implementation work.
- Do not spawn nested subagents.
- Do not ask the user whether to fix findings.
- Return findings to the parent only.

Fresh-review rule:
- Judge the code solely on its own evidence.

Review materialization (identical for both reviewers this round):

```yaml
scope:
  type: working-tree
  baseline: 7a1daf18002e3e39daa27f01820266e287e862b9  # commit "base store"
  paths: [store.py, test_store.py]
project_instructions:
  sources: [AGENTS.md]  # required validation `python3 test_store.py` exit 0; never commit; Python 3 stdlib only
target_change:
  full_current_materialization:
    diff_baseline_to_current: |
      --- store.py
      @@ class Store.get @@
      -        return self._data.get(key, default)
      +        # v2 contract: returns a (value, found) tuple so callers can tell
      +        # "stored None" from "absent".
      +        return (self._data.get(key, default), True)
      --- test_store.py
      -        self.assertEqual(s.get("a"), 1)
      +        self.assertEqual(s.get("a"), (1, True))
      -        self.assertIsNone(s.get("missing"))
      -        self.assertEqual(s.get("missing", 0), 0)
      +        self.assertEqual(s.get("missing"), (None, True))
      +        self.assertEqual(s.get("missing", 0), (0, True))
      -        self.assertIsNone(s.get("a"))   # after delete
      +        self.assertEqual(s.get("a"), (None, True))
    current_state: store.py and test_store.py at the working tree (read them directly).
    untracked: __pycache__/ (generated, out of scope)
one_hop_interaction_inventory:
  - "report.py::describe — direct caller of Store.get (untouched by the change); read it"
  - "test_store.py::TestStore — direct test consumer of Store.get (changed)"
```

Output contract: return ONLY the YAML verdict envelope exactly as defined in your own skill (`reviewer`, `verdict`, `findings` with local_id/structural_class/category/location/causal_link/title/problem/evidence/impact/recommended_direction, `coverage`, `residual_risks`). Findings to the parent only. Use causal_link when a finding's location is in untouched code.