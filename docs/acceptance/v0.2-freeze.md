# v0.2 Candidate Freeze Manifest

## Release closeout

- **Status**: accepted for v0.2 release with explicit owner waivers, 2026-09-11.
- **Frozen plugin subtree**: `8655c6a2afc43e88489d80aaf487ca95a7680625`
  (unchanged since `bffa075`).
- **Executed behavior evidence**: E05/E07/E08/E09 decision procedures re-run on
  re-freeze 2; E06 passed in pi and in the Claude Code release runtime with the
  plugin's own agents; both E10 variants passed in pi.
- **Release-matrix breadth waiver**: the owner directed the team to stop repetitive
  live validation. E01–E04 and the remaining Claude/Codex release cells were not run
  on re-freeze 2. They are waived for v0.2, not represented as PASS.
- **Known residuals**: Codex-side one-hop materialization has no v0.2 live release
  run; E10-v1 reached the required outcome but its pi driver conflated envelope retry
  with repair and normalized one prose-prefixed retry (documented in the transcript).
- **Budget waiver**: plugin-level deferred cost is 16,963 tokens; all three skills
  remain 100/100 with good invoke bands. See `plugin-eval-budget-waiver.md` for final
  measurements and observed-usage limitations.

Final local gates at closeout:

- `python3 scripts/check_repo.py` → exit 0;
- unittest discovery → 37 tests, OK;
- `claude plugin validate plugins/dual-review-loop` → passed;
- both manifests at 0.2.0 and legal files present;
- plugin-eval → plugin 86/B with the single waived deferred fail; three skills
  100/100 with no fail or warning;
- `git diff --check` and `git fsck --no-progress` → exit 0.

Remote CI is evaluated after the authorized push and does not retroactively change
the frozen plugin subtree.

## Re-freeze 2 (current) — materialization amendment

- **Date**: 2026-09-11
- **Commit**: `bffa075` (full: see git log)
- **Plugin subtree**: `8655c6a2afc43e88489d80aaf487ca95a7680625`
- **Amended file**: `reviewer-prompt-contract.md` sha256[:16] `d39c6163e2d0eec8`
  (one-hop interaction inventory added to §3; spawn checklist §4 updated)
- **Reason**: E06 diagnostic live run (see `transcripts/v0.2/e06-cross-boundary-live.md`)
  proved the materialization starves reviewers of one-hop caller code — the E06
  regression was unreportable. Amendment is the owner-approved narrowed version
  (inventory + supply rules + incomplete-materialization stop).
- **Invalidates**: all prior behavioral evidence. Decision-procedures E05/E07/E08/E09
  re-executed against this text (see their v0.2 transcripts, re-run sections).
  E06 was subsequently re-run successfully in pi and in the Claude Code release
  runtime using the plugin's own reviewer agents.
- **Gates at re-freeze**: checker OK; 37 unittests OK; Claude validator passed;
  `git diff --check` clean.

## Original freeze — superseded

- **Date**: 2026-09-10
- **Commit**: `943a02c67c082dfa7bc876585bb7dc406ee070c1` (`943a02c`)
- **Tree**: `d2b510330f96614dafc25aab1c810280e8a7661d`
- **Plugin subtree** (`plugins/dual-review-loop`): `b2af293a0179426c9d425befbb4ec0d8b213fbe2`

## Rule

All v0.2 behavioral evidence after re-freeze 2 refers to this frozen candidate. If the
plugin subtree hash changes, the evidence must be re-evaluated. Only acceptance
material (transcripts, prep docs, runbook status) was added after the freeze.

## Original-freeze static gates (historical)

- `python3 scripts/check_repo.py` → `check_repo: OK (packaging, protocol files, projections, schema)`, exit 0
- `python3 -m unittest discover -s tests -p 'test_*.py'` → 36 tests, OK
- `claude plugin validate plugins/dual-review-loop` → Validation passed
- `git diff --check` → clean

## Original-freeze per-file sha256 (historical, first 16 hex chars)

```
e2f2744304a541f3  plugins/dual-review-loop/plugin.json
2868ec10f326b632  plugins/dual-review-loop/.claude-plugin/plugin.json
b26ba9812dc12e45  plugins/dual-review-loop/skills/dual-review-loop/SKILL.md
831f8f9c13519930  plugins/dual-review-loop/skills/dual-review-correctness/SKILL.md
e9746dbaf07af0bd  plugins/dual-review-loop/skills/dual-review-structure/SKILL.md
c622aae70955c15f  plugins/dual-review-loop/skills/dual-review-loop/references/review-scope.md
98e94720c10b1380  plugins/dual-review-loop/skills/dual-review-loop/references/reviewer-prompt-contract.md
d2f68a859ee30e84  plugins/dual-review-loop/skills/dual-review-loop/references/finding-schema.md
2ca89d63798a3b97  plugins/dual-review-loop/skills/dual-review-loop/references/convergence-contract.md
9762a54c8ac0d158  plugins/dual-review-loop/skills/dual-review-loop/references/output-format.md
8abf1bfd2a58a83d  plugins/dual-review-loop/agents/dual-review-correctness-reviewer.md
ff5069c3d9265bdc  plugins/dual-review-loop/agents/dual-review-structure-reviewer.md
```

## Fixture verification at freeze

- `plant.py` key `X` builds correctly; runtime check executed:
  `describe(s, 'missing')` → `'missing: (None, True)'` (R2 defect reproduced on
  the planted tree; validation green — regression is behavioral, not test-visible).
- E10 synthetic variants constructed and verified (see `transcripts/v0.2/live-run-prep.md`):
  - v1 change-caused: current RED (3 failures), base GREEN.
  - v2 pre-existing: RED (2 identical failures) at base `d5b57d8` and current.
